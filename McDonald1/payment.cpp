#include "payment.h"

#include "template.h"
#include <QVBoxLayout>
#include <QHBoxLayout>
#include <QPushButton>
#include <QLabel>
#include <iostream>

/*Payment::Payment(Template * parent, Model * m) : QWidget(parent)
{
    model = m;
    temp = parent;
    QHBoxLayout * mainLayout = new QHBoxLayout();
    QHBoxLayout * topLayout = new QHBoxLayout();
    topLayout->setSpacing(0);


    QPushButton * cardButton = new QPushButton("Card");//, model);
    //ChangeButtonSize(boissonsButton);

    QPushButton * cashButton = new QPushButton("Cash");

    mainLayout->addWidget(cardButton);
    mainLayout->addWidget(cashButton);

}



void Payment::paintEvent(QPaintEvent *){
}*/

